name: "TODO"

on: ["push"]

jobs:
  test:
    runs-on: "macOS-latest"
    steps:
        - name: "TODO to Issue"
          uses: "alstr/todo-to-issue-action@v4"
          id: "todo"
          with:
            TOKEN: ${{ secrets.GITHUB_TOKEN }}
