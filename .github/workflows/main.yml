name: CI

on: ["push"]

env:
  DEVELOPER_DIR: /Applications/Xcode_14.0.1app

jobs:
  test:
    runs-on: macOS-latest
    steps:
        - uses: "actions/checkout@master"
        - name: "TODO to Issue"
        uses: "alstr/todo-to-issue-action@v4"
        id: "todo"
        with:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
